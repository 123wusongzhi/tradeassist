# Deny public access to internal metrics endpoint
location /internal/metrics {
    deny all;
    allow 127.0.0.1;
    allow 10.0.0.0/8;
    allow 192.168.0.0/16;
    proxy_pass http://trademind_api;
    proxy_read_timeout 10s;
}
