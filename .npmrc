# Umi / Ant Design Pro resolve peer & hoisted deps more reliably with hoisting.
shamefully-hoist=true
strict-peer-dependencies=false

# Dependency installation must not fetch Playwright browser binaries.
# Explicit `playwright install ...` recovery/test commands remain available.
playwright_skip_browser_download=true
