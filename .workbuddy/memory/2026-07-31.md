# 2026-07-31

## OpenCLI 插件完整扫描与兼容性验证

- 对 TradeMind 项目中内置的 OpenCLI 插件进行了全面扫描与兼容性验证
- 全局已安装 `@jackwener/opencli v1.8.6`（Node.js v22.22.2，满足 >=20.0.0 要求）
- `opencli doctor` 全部通过：Daemon running (PID 26216, port 19825)，Extension v1.0.22 connected，Profile xynwjrgw connected
- 内置适配器标记为 `@trademind-managed-opencli-adapter v1`，MD5 与 ~/.opencli/clis/tmall/ 一致
- 适配器安装脚本 `install-opencli-adapter.ts` 运行正常，显示"already up to date"
- 已生成插件压缩包 `artifacts/opencli-plugin-package.zip`（24KB），包含 adapter/bridge/driver/scripts
- 结论：当前环境 OpenCLI 可正常使用

## 浏览器扩展压缩包 & Admin 前端重建

- 已生成浏览器扩展压缩包 `artifacts/browser-extension.zip`（16KB），来自 `browser-extension/dist/`
- Admin 前端页面 `/collect/browser-extension`（浏览器扩展）源码和路由已完整
- 重建 Admin 时遇到 safe-delete shim 拦截 unlinkSync 导致 umi 清理 dist 失败
- 解决方法：手动 `rm -rf admin/dist` 后再 `npx max build`，编译成功（1.19min）
- 构建产物 `p__Collect__BrowserExtension__index.async.js`（17KB）已生成

## Docker 容器热更新修复 404

- 用户访问 `http://127.0.0.1:8000/collect/browser-extension` 时接口返回 404
- 原因：Docker 容器里的 backend 是昨天构建的，没有 collectextension 路由
- Docker image build 因网络拉取基础镜像失败，改用宿主机交叉编译方案：
  1. `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build` 在宿主机编译 Linux 后端
  2. `docker cp` 将新二进制复制进 `trademind-full-backend-1:/app/`
  3. `docker restart` 重启容器
- 验证：接口从 404 变为 401（需要登录），路由已注册成功
